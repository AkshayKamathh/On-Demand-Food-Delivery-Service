services:
  frontend:
    build:
      context: .
      dockerfile: dockerfile
    container_name: ofs-frontend
    ports:
      - "3000:3000"
    volumes:
      - ./ofs-frontend:/app
      - /app/node_modules
      - /app/.next
    environment:
      NODE_ENV: development
      WATCHPACK_POLLING: "true"
    command: npm run dev
